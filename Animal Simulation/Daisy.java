public class Daisy extends Plant {
    
    /**
     * Create a new daisy at in the field.
     * 
     * @param sim The main simulator object.
     */
    public Daisy(Simulator sim){
        super(sim);
        setFoodValue(3);
    }
}
